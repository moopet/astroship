---
export interface TextProps {
  text: string;
}

export interface Props {
  item: TextProps;
}

const { item } = Astro.props;
console.log("text template");
console.log(item.text.value.document);
---

<!-- <div set:html={item.text.value.document} /> -->
